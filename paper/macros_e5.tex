% auto-generated by scripts/p13_e5_table_fair.py -- do not hand-edit
\newcommand{\EfivePdrDelta}{1.5}\newcommand{\EfivePdrLo}{+1.30}\newcommand{\EfivePdrHi}{+1.77}  % policy vs BEST SINGLE rule: mean per-instance relative gap, 95% paired-bootstrap CI
\newcommand{\EfivePortDelta}{1.4}\newcommand{\EfivePortLo}{+1.20}\newcommand{\EfivePortHi}{+1.65}  % policy vs the per-instance PORTFOLIO: mean per-instance relative gap, 95% paired-bootstrap CI
\newcommand{\EfiveGaDelta}{0.7}\newcommand{\EfiveGaLo}{+0.49}\newcommand{\EfiveGaHi}{+0.96}  % policy vs GA-30s: mean per-instance relative gap, 95% paired-bootstrap CI
\newcommand{\EfiveRegN}{+1.43}\newcommand{\EfiveRegNLo}{+0.99}\newcommand{\EfiveRegNHi}{+1.88}  % N: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (excludes 0)
\newcommand{\EfiveRegL}{+1.80}\newcommand{\EfiveRegLLo}{+1.22}\newcommand{\EfiveRegLHi}{+2.41}  % L: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (excludes 0)
\newcommand{\EfiveRegS}{+0.55}\newcommand{\EfiveRegSLo}{-0.03}\newcommand{\EfiveRegSHi}{+1.12}  % S: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (COVERS 0)
\newcommand{\EfiveRegW}{+1.77}\newcommand{\EfiveRegWLo}{+1.11}\newcommand{\EfiveRegWHi}{+2.44}  % W: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (excludes 0)
\newcommand{\EfiveRegLS}{+1.56}\newcommand{\EfiveRegLSLo}{+1.06}\newcommand{\EfiveRegLSHi}{+2.09}  % LS: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (excludes 0)
\newcommand{\EfiveRegLW}{+2.08}\newcommand{\EfiveRegLWLo}{+1.42}\newcommand{\EfiveRegLWHi}{+2.76}  % LW: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (excludes 0)
\newcommand{\EfiveRegSW}{+1.10}\newcommand{\EfiveRegSWLo}{+0.53}\newcommand{\EfiveRegSWHi}{+1.70}  % SW: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (excludes 0)
\newcommand{\EfiveRegLSW}{+1.27}\newcommand{\EfiveRegLSWLo}{+0.69}\newcommand{\EfiveRegLSWHi}{+1.89}  % LSW: policy lead over best single rule, mean per-instance relative gap, 95% paired-bootstrap CI (excludes 0)
\newcommand{\EfiveBeatsPdrSig}{5}  % training regimes (of 6) whose per-regime CI over the best single rule EXCLUDES zero
\newcommand{\EfiveBeatsPdr}{6}  % training regimes (of 6) where the policy beats the best single rule
\newcommand{\EfiveBeatsPort}{6}  % ... where it beats the per-instance best-of-four PORTFOLIO
\newcommand{\EfiveBeatsGa}{5}  % ... where it beats the GA
\newcommand{\GaPolicyAhead}{7}\newcommand{\GaRegimes}{8}  % regimes (of all 8, held-out included) where the policy beats the GA
\newcommand{\GaAheadN}{1}\newcommand{\GaAheadReg}{S}\newcommand{\GaAheadDelta}{0.94}  % ... and where the GA beats the policy (S, by 0.94%)
\newcommand{\GaPolicyLeadLo}{0.16}\newcommand{\GaPolicyLeadHi}{1.64}  % the policy's lead over the GA on the regimes it leads, min and max (%)
\newcommand{\EfiveTrainN}{6}
\newcommand{\EfiveDaggerN}{4}  % regimes where the 5s search improved on its warm start on ZERO instances
\newcommand{\EfiveCpFiveAhead}{4}  % of the 4 daggered regimes, how many the policy leads at 5s
\newcommand{\EfiveCpThirtySolver}{8}\newcommand{\EfiveAllN}{8}  % regimes (of 8) where the 30s solver has passed the policy
\newcommand{\CpThreeHundredN}{30}  % instances per regime in the 300s tier
\newcommand{\RefLOpt}{25}  % instances proved optimal at 300s, of 30 (regime L)
\newcommand{\RefLWOpt}{15}  % instances proved optimal at 300s, of 30 (regime LW)
\newcommand{\CpMwkrSthreehundred}{208.0}  % MWKR-seeded CP-SAT @300s on S, credited min(hint, search) as everywhere else; raw search alone reads 211.7
\newcommand{\PolicySec}{0.61}  % worst-regime median s/instance, 4 CPU cores, model resident
\newcommand{\PolicyCores}{4}
\newcommand{\CpFiveVsPolicy}{8}  % the 5s rung, in units of the policy runtime
\newcommand{\GaSec}{30}  % the GA wall-clock budget per instance (eval_ga_family.py, --budget)
\newcommand{\GaVsPolicy}{49}  % the GA budget, in units of the policy runtime
